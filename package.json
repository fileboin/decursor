{
  "name": "decursor",
  "version": "0.1.0",
  "type": "module",
  "description": "AI-powered browser code editor with MCP support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "postinstall": "pip3 install mcp-server-git --quiet 2>/dev/null || echo 'Note: pip3 not available — Git MCP server disabled'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.5.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@modelcontextprotocol/server-filesystem": "^2026.1.14",
    "@modelcontextprotocol/server-github": "^2025.4.8",
    "@modelcontextprotocol/server-memory": "^2026.1.26",
    "@modelcontextprotocol/server-brave-search": "^0.6.2"
  },
  "engines": {
    "node": ">=18"
  }
}
